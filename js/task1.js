const name = 'Генератор защитного поля';
let price = 1000;
    
const message = `Выбран «${name}», цена за штуку ${price} кредитов`;
console.log(message);
    
if (price = 2000) {
    const message = `Выбран «${name}», цена за штуку ${price} кредитов`;
    console.log(message); 
}
